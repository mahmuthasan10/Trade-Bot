# Master Trading Bot v3.0 - Faz 1 + Faz 2: Altyapı + Strateji Motoru
# Python 3.11+ zorunludur

# ── Async Çekirdek ───────────────────────────────────────────────
asyncio-mqtt>=0.16.1          # Async pattern referansı
uvloop>=0.19.0; sys_platform != 'win32'  # Linux/Mac async hızlandırma

# ── Borsa Entegrasyonu ───────────────────────────────────────────
ccxt>=4.2.0                   # REST ve temel entegrasyonlar
# Not: ccxt.pro lisanslı dağıtımdır; erişimin varsa özel wheel/tarball olarak kurulur.
alpaca-py>=0.21.0             # US Stocks async feed

# ── Redis (Async) ────────────────────────────────────────────────
redis[hiredis]>=5.0.0         # hiredis C parser ile hızlı async redis

# ── PostgreSQL / TimescaleDB (Async) ─────────────────────────────
asyncpg>=0.29.0               # Async PostgreSQL driver
sqlalchemy[asyncio]>=2.0.0    # ORM (opsiyonel, raw query de kullanılabilir)

# ── Veri İşleme ──────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0

# ── Konfigürasyon ────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Test ─────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0

# ── Faz 2: Teknik İndikatörler ──────────────────────────────────
pandas-ta>=0.3.14b1             # TA-Lib alternatifi (pure Python, kolay kurulum)
